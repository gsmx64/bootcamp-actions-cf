name: Deploy to server

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    
    env:
     NAME: CodigoFacilito
     
    steps:
     - name: Hola Mundo
       run: echo "Hola Mundo $CodigoFacilito"

  second_job:
    needs: first_job
    runs-on: ubuntu-latest

    steps:
     - name: Hola Mundo
       run: echo "Hola Mundo ${{ github.ref }}"